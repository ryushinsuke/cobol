#! /bin/sh
#-----------------------------------------------------------------------------------------
# システムＩＤ      ：  T-STAR
# システム名称      ：  共通
# モジュールＩＤ    ：  BCZY0001
# モジュール名称    ：  顧問系バッチログファイルリネーム
# 処理概要          ：  顧問系バッチログファイルリネーム
# 引数              ：  なし
# リターンコード    ：
#
# 改訂履歴
# 年月日    区分    所属    内容
# --------  ----    ------  --------------------------------------
# 20110414  新規    SCS     新規作成
#
# Copyright (C) 2006 by Nomura Research Institute,Ltd.
# All rights reserved.
#-----------------------------------------------------------------------------------------

TS_LOGFILE=${LogFile}

#-----------------------------------------------------------------------------------------
#9世代存在の時、削除する
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.9" ];then
rm -f ${TS_LOGFILE}.9
fi

#-----------------------------------------------------------------------------------------
#8世代存在の時、8世代→9世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.8" ];then
mv ${TS_LOGFILE}.8 ${TS_LOGFILE}.9
fi

#-----------------------------------------------------------------------------------------
#7世代存在の時、7世代→8世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.7" ];then
mv ${TS_LOGFILE}.7 ${TS_LOGFILE}.8
fi

#-----------------------------------------------------------------------------------------
#6世代存在の時、6世代→7世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.6" ];then
mv ${TS_LOGFILE}.6 ${TS_LOGFILE}.7
fi

#-----------------------------------------------------------------------------------------
#5世代存在の時、5世代→6世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.5" ];then
mv ${TS_LOGFILE}.5 ${TS_LOGFILE}.6
fi

#-----------------------------------------------------------------------------------------
#4世代存在の時、4世代→5世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.4" ];then
mv ${TS_LOGFILE}.4 ${TS_LOGFILE}.5
fi

#-----------------------------------------------------------------------------------------
#3世代存在の時、3世代→4世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.3" ];then
mv ${TS_LOGFILE}.3 ${TS_LOGFILE}.4
fi

#-----------------------------------------------------------------------------------------
#2世代存在の時、2世代→3世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.2" ];then
mv ${TS_LOGFILE}.2 ${TS_LOGFILE}.3
fi

#-----------------------------------------------------------------------------------------
#1世代存在の時、1世代→2世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}.1" ];then
mv ${TS_LOGFILE}.1 ${TS_LOGFILE}.2
fi

#-----------------------------------------------------------------------------------------
#0世代存在の時、0世代→1世代
#-----------------------------------------------------------------------------------------
if [ -r "${TS_LOGFILE}" ];then
mv ${TS_LOGFILE} ${TS_LOGFILE}.1
fi
